server:
  port: 8081
spring:
  application:
    name: fraud
  datasource:
    username: 'amigoscode'
    url: jdbc:postgresql://127.0.0.1:5432/fraud
    password: 'password'
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true